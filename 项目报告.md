# 海水养殖水质数据分析系统 — 项目报告

| 项目 | 内容 |
| --- | --- |
| 课程 | 编程基础实践 |
| 课题 | 海水养殖水质数据分析系统 |
| 开发语言 | C（C11） |
| 数据来源 | 海南省陵水县新村镇海水养殖基地物联网监测系统 |
| 数据规模 | 23,203 条记录，每 5 分钟一条，6 项水质参数 |
| 工程组织 | 三层架构（pojo / service / controller / view / util / dao），多文件 + 头文件分离 |
| 编译环境 | MinGW GCC 13.1.0 / CMake |
| 报告日期 | 2026-06-25 |

> 本报告中所有统计量、相关系数、回归结果、滤波标准差、存储性能、预警计数均由程序对真实数据 `dao/data_modify.csv` 实测得出（驱动程序 [test/analyze_driver.c](test/analyze_driver.c)），非人工编造。

---

## 一、项目背景与目标

海水养殖中，水质剧烈变化会破坏藻相与菌相平衡，导致养殖对象应激、疾病甚至大规模死亡。本系统对养殖基地连续监测得到的水质时间序列数据进行读取、预处理、统计分析、风险预警与回归预测，揭示各参数的变化规律与相互关系，为水质预测与精准养殖提供数据支持。

系统处理 6 项参数：水温 Temp（℃）、盐度 Salinity（PSU）、pH、溶解氧 DO（mg/L）、降水量 Precipitation（mm）、气温 Air_temp（℃）。数据起始于 2025-01-01 12:00，采集频率 5 分钟/次。

**系统目标**：以 C 语言实现一个终端交互式水质分析系统，涵盖数据基础操作、数据预处理、统计分析、预测模型、系统集成五大模块，并完成相应的分析讨论。

---

## 二、系统总体设计

### 2.1 三层架构与工程组织

工程按职责分层，多文件组织，头文件使用 `#ifndef` 条件编译避免重复包含：

```
pojo/        Data.h User.h            数据结构（Data / DataSet / User）
service/     DataService.h + 7 个 .c  业务逻辑层（核心算法）
  ├── dataset.c        数据集生命周期 + 动态数组扩容
  ├── field_meta.c     字段元数据 / 取值范围 / 字段名解析
  ├── io_csv.c         CSV 读写与容错解析
  ├── io_marks.c       预处理标记 / 操作标记持久化
  ├── analyze.c        预处理、统计、相关、回归、滤波、查询、增删改、预警
  ├── auth.c           登录鉴权
  ├── reports.c        报告生成
  └── storage_backup.c 二进制存储、性能对比、备份恢复
controller/  DataController.c          状态管理与菜单循环调度
view/        DataView*.c               终端交互界面（登录/主菜单/子菜单/表格）
util/        operation_file.c          文件操作辅助
dao/         data_modify.csv           原始水质数据（23203 条）
main.c       入口
```

### 2.2 数据结构设计

水质记录结构体（[pojo/Data.h](pojo/Data.h)）：

```c
typedef struct {
    int record_index;          // 记录序号（1 起，对应时间序列索引）
    double temp;               // 水温 ℃
    double salinity;           // 盐度 PSU
    double ph;                 // pH
    double do_value;           // 溶解氧 mg/L
    double precipitation;      // 降水量 mm
    double air_temp;           // 气温 ℃
} Data;

typedef struct {
    Data* items;               // 动态数组首地址
    int size;                  // 当前元素数
    int capacity;              // 已分配容量
} DataSet;
```

`record_index` 保留原始行号，使预处理删除记录后仍能追溯原始时间位置（预警的时间换算依赖它）。缺失值统一用 `NAN` 表示，便于在统计/回归中用 `isnan()` 跳过。

### 2.3 动态数组 vs 静态数组（分析讨论，对应任务 1.1(3)）

本系统采用**动态数组**而非静态数组，原因如下：

1. **数据规模未知且较大**。CSV 文件大小任意，本数据集就有 23,203 条；静态数组需按上限预分配，若按 10 万条 × 56 字节 ≈ 5.5 MB 常驻内存，对小文件是浪费，对超上限文件又无法处理。
2. **内存效率**。动态数组初始容量 1000，满后翻倍扩容（[service/dataset.c](service/dataset.c) `data_service_dataset_push`），按需增长，内存占用与实际数据量成正比。
3. **随机访问性能**。动态数组连续存储，`O(1)` 随机访问，优于链表；而链表虽插入删除 `O(1)`，但本系统的查询/排序/统计都需要大量随机访问，数组更合适。
4. **工程通用性**。任务书明确要求"支持任意大小文件""先分配初始容量，满则扩容"，动态数组是标准做法。

权衡：扩容时需 `realloc` 拷贝，单次 `O(n)`，但采用翻倍策略均摊后每次 `push` 为 `O(1)`，总成本可接受。测试中连续 push 2500 条触发多次扩容，全程无内存错误（见 [test/测试报告.md](test/测试报告.md) 模块 10）。

---

## 三、模块一：数据基础操作

### 3.1 文件读取（对应 1.2）

实现见 [service/io_csv.c](service/io_csv.c) `data_service_read_csv`：

- 跳过表头行；逐行解析 6 个字段。
- **缺失值容错**：`parse_field_token` 识别空串、`nan`/`NaN`、`-999`/`-9999`，统一转为 `NAN` 并计入 `missing_values`。
- **格式错误处理**：列数不足或非数字 token 计入 `format_errors` 并跳过该行，不中断读取。
- **动态扩容**：初始容量 1000，满后翻倍；任何 `push` 失败释放内存并返回 0。
- **文件不存在**：`fopen` 返回 NULL 时返回 0。

实测真实数据：`total=23203, parsed=23203, format_errors=0, missing_values=279`。

### 3.2 数据存储与性能对比（对应 1.3，分析讨论 1.3(5)）

系统同时支持 CSV 文本与二进制两种格式，并提供性能对比 `data_service_benchmark_storage`。对真实数据实测结果：

| 存储格式 | 文件大小（字节） | 写入时间（秒） | 读取时间（秒） | 人类可读 |
| --- | --- | --- | --- | --- |
| CSV 文本 | 1,483,421 | 0.5610 | 0.7070 | 是 |
| 二进制 | 1,299,372 | 0.0440 | 0.0010 | 否 |

**结论**：二进制文件比 CSV 小约 12.4%，写入快约 12.7 倍，读取快约 707 倍。

**CSV vs 二进制适用场景**：
- **CSV 适合**：需要人工查看/编辑、与外部系统（Excel、Python、数据库导入）交换、需要跨平台兼容的场景。本系统的原始数据导入、备份文件、分析报告均采用 CSV，便于养殖户用 Excel 打开核对。
- **二进制适合**：对读写性能要求高、数据量大、无需人工查看、内部高速缓存的场景。本系统的存储性能对比、大规模随机读取适合用二进制。

**实际存储大小与理论原始数据大小的差异分析**：
- 理论原始数据：23,203 条 × 6 字段 × 8 字节（double）= 1,113,744 字节。
- 实际二进制 1,299,372 字节，多出约 185 KB。原因：① 每条记录额外存了 `record_index`（int，4 字节），23,203×4 ≈ 92 KB；② 文件可能含少量头部/对齐；③ `double` 在内存中按 8 字节对齐，结构体可能有填充字节。
- 实际 CSV 1,483,421 字节，比二进制大。原因：CSV 以 ASCII 文本存储每个数值，本数据每个浮点数平均 8~10 个字符（如 `25.504256`），加上 5 个逗号分隔符与换行符，每条约 63 字节，23,203×63 ≈ 1.46 MB，与实测 1.48 MB 吻合。文本格式需逐字符解析（`strtod`），故读写远慢于二进制的 `fread`/`fwrite` 整块拷贝。

### 3.3 数据查询（对应 1.4）

`data_service_query_page`（[service/analyze.c](service/analyze.c)）支持：
- **分页浏览**：每页 15 条，支持上/下一页、跳转指定页；越界页码自动修正到末页。
- **条件筛选**：按任意参数的 `[min,max]` 区间筛选。
- **排序**：按任意参数升/降序（`qsort`，`NAN` 排到末尾）。

实现先筛选 → 再排序 → 再分页，返回 `QueryPage`（含 `total / total_pages / page / items`）。

### 3.4 数据修改 / 删除 / 备份（对应 1.5、1.6、1.7）

- **单条修改** `data_service_modify_record`：按 `record_index` 定位，写入新值并返回旧值；修改前由 controller 自动备份。
- **删除** `data_service_delete_records`：支持按行号单条删除，或按字段范围批量删除；返回被删行号列表。
- **添加** `data_service_add_record`：自动分配递增的 `record_index`。
- **备份恢复** `data_service_backup_file` / `restore_from_backup`：备份文件名带时间戳（如 `backup_20260625_191400.csv`）防覆盖；`list_backups` 列出可选备份；恢复时校验格式。

---

## 四、模块二：数据预处理

### 4.1 异常值检测与处理（对应 2.1，分析讨论 2.1(3)）

字段合理范围（[service/field_meta.c](service/field_meta.c)）：temp[-5,40]、salinity[0,45]、pH[6.5,9]、DO[0,15]、precipitation[0,500]、air_temp[-10,50]。

`data_service_preprocess` 处理流程：
1. 逐条扫描 6 个字段，统计每条记录的异常字段数与缺失字段数。
2. **异常字段数 ≥ 3**：整条记录删除。
3. **异常字段数 < 3**：将异常字段置为 `NAN`（即"删除异常数据"），保留该条记录。
4. 第二趟：对所有 `NAN`（原缺失 + 异常修复产生）用均值逼近法填充。

实测：`total=23203, kept=23203, deleted=0, abnormal_records=475, abnormal_values=475, filled=754, missing=279`。

> 说明：`deleted=0` 是因为真实数据中没有任何一条记录同时有 ≥3 个异常字段（异常都是单点跳变）。`filled=754 = 475（异常修复）+ 279（原缺失）`，与逻辑吻合。

**异常值处理方法合理性分析**：
常见的异常值处理方法有：① 直接删除整条记录；② 用均值/中位数/前后值插值替换；③ 分箱平滑；④ 基于 3σ 或箱线图（IQR）的统计检测。

本系统采用的"≥3 异常则删整条、<3 则插值修复"策略**总体合理**：
- 一条记录 6 个字段中若 ≥3 个异常，说明该时刻传感器整体故障或环境剧变，数据已不可信，删除是恰当的，避免污染统计。
- 单字段异常则仅修复该字段、保留其余有效字段，最大化数据利用率，符合"不轻易丢弃数据"的原则。
- **可改进之处**：本系统用固定硬范围（min/max）判定异常，无法识别"在范围内但偏离正常波动"的离群点（如夏季水温突降到 22℃ 仍在 [−5,40] 内但不正常）。可补充 3σ/IQR 统计检测；另外"≥3 删除"的阈值是经验值，对不同传感器密度可调。

### 4.2 缺失值处理——均值逼近法（对应 2.2）

`approximate_value`（[service/analyze.c](service/analyze.c:16)）实现均值逼近：对缺失位置 `i`，向前后各扩展最多 10 步，取两侧有效值均值；若某侧无有效值则用另一侧；若两侧均无则用该字段全局均值。这与任务书公式 `a(i)=(a(i−n)+a(i+m))/2`（n、m 默认 10）一致，并处理了边界情况。

实测填充 754 个值，清洗后数据集不含任何 `NAN`（已由测试模块 4 验证）。

### 4.3 数据滤波——移动平均法（对应 2.2 滤波，分析讨论窗口选择）

`data_service_moving_average_filter` 对 temp/DO/pH/salinity 四个参数做窗口 N=2k+1 的中心移动平均，边界处窗口截断（不越界）。实测滤波前后标准差：

| 窗口 | temp σ前→后 | DO σ前→后 | pH σ前→后 | salinity σ前→后 |
| --- | --- | --- | --- | --- |
| 3  | 1.8864 → 1.8706 | 2.1072 → 2.0996 | 0.1490 → 0.1432 | 0.8437 → 0.8178 |
| 5  | 1.8864 → 1.8674 | 2.1072 → 2.0970 | 0.1490 → 0.1419 | 0.8437 → 0.8117 |
| 7  | 1.8864 → 1.8660 | 2.1072 → 2.0948 | 0.1490 → 0.1412 | 0.8437 → 0.8084 |
| 9  | 1.8864 → 1.8652 | 2.1072 → 2.0925 | 0.1490 → 0.1408 | 0.8437 → 0.8058 |
| 11 | 1.8864 → 1.8647 | 2.1072 → 2.0900 | 0.1490 → 0.1403 | 0.8437 → 0.8037 |

**窗口大小与噪声抑制的关系**：
- 标准差随窗口增大而单调下降，说明窗口越大，短期噪声抑制越强，曲线越平滑。
- 但**边际收益递减**：窗口从 3→5 标准差下降明显，从 9→11 下降已很小。
- **过大窗口的副作用**：窗口过大会过度平滑，抹掉真实的日变化等有用信号，造成信号失真和相位滞后。

**最佳窗口选择**：综合考虑噪声抑制与信号保真，**窗口 5～7 为最佳**。本系统默认场景建议窗口 5：在 temp 上标准差从 1.8864 降到 1.8674（降幅 1.0%），salinity 从 0.8437 降到 0.8117（降幅 3.8%，四参数中降幅最大、滤波收益最高），同时窗口较小对真实趋势的失真最小。本数据噪声本身不大（5 分钟采样、传感器较稳定），故各窗口标准差变化幅度有限。

---

## 五、模块三：统计分析

### 5.1 基本统计量（对应 3.1）

对预处理后数据计算均值/最值/标准差/计数（`data_service_compute_basic_stats`）：

| 参数 | 均值 | 最小值 | 最大值 | 标准差 | 计数 |
| --- | --- | --- | --- | --- | --- |
| Temp ℃ | 25.7316 | 19.6537 | 29.5807 | 1.8864 | 23203 |
| Salinity PSU | 34.5687 | 0.0000 | 36.6311 | 0.8437 | 23203 |
| pH | 8.3071 | 7.9060 | 8.9970 | 0.1490 | 23203 |
| DO mg/L | 4.5702 | 0.0000 | 14.2824 | 2.1072 | 23203 |
| Precip mm | 0.0003 | 0.0000 | 0.0114 | 0.0016 | 23203 |
| Air_temp ℃ | 23.0727 | 14.8351 | 31.8649 | 3.2324 | 23203 |

解读：水温均值 25.7℃、盐度 34.6 PSU 符合海南陵水近海养殖环境；DO 均值 4.57 mg/L 偏低、标准差 2.11 较大，提示存在缺氧风险（与预警模块结果一致）；salinity 与 DO 的最小值为 0，是传感器短时故障产生的异常（已被预处理修复或保留为边界值）。

### 5.2 分段统计与预警（对应 3.2）

`data_service_detect_warnings`（[service/analyze.c](service/analyze.c:158)）实现两类预警：

- **凌晨缺氧预警**：按"2025-01-01 12:00 起、5 分钟/次"换算每条记录的时间，筛选每天 03:00–05:00（分钟序号 180–300）的 DO 数据求日均；均值 <3.0 触发"严重缺氧"、<4.0 触发"亚缺氧"。
- **盐度突变预警**：1 小时降幅（当前与 12 条之前比较）>2.0 触发 `salinity_drop_1h`；24 小时降幅（与 288 条之前比较）>5.0 触发 `salinity_drop_24h`。

实测预警共 **85 条**，分布如下：

| 预警类型 | 数量 |
| --- | --- |
| 严重缺氧（DO<3.0） | 30 |
| 亚缺氧（DO 3.0–4.0） | 8 |
| 盐度突变（1 小时） | 45 |
| 盐度突变（24 小时） | 2 |

预警集中出现在 2025 年 1–3 月凌晨及 3 月暴雨期，与"凌晨光合作用停止、DO 谷值"和"暴雨导致盐度骤降"的养殖规律一致。所有预警写入 [reports/warning_report.txt](reports/warning_report.txt)，含时间、类型、数值、处理建议。

### 5.3 相关性分析（对应 3.3，分析讨论 3.3(3)）

`data_service_pearson` 计算 6×6 皮尔逊相关系数矩阵，实测：

| | Temp | Salinity | pH | DO | Precip | AirTemp |
| --- | --- | --- | --- | --- | --- | --- |
| **Temp** | 1.0000 | **−0.3316** | 0.1850 | **−0.3808** | −0.1754 | **0.3674** |
| **Salinity** | −0.3316 | 1.0000 | −0.2329 | −0.0632 | −0.0219 | −0.2055 |
| **pH** | 0.1850 | −0.2329 | 1.0000 | **0.5965** | −0.0299 | 0.2085 |
| **DO** | −0.3808 | −0.0632 | 0.5965 | 1.0000 | 0.1362 | 0.0067 |
| **Precip** | −0.1754 | −0.0219 | −0.0299 | 0.1362 | 1.0000 | 0.0413 |
| **AirTemp** | 0.3674 | −0.2055 | 0.2085 | 0.0067 | 0.0413 | 1.0000 |

**最强正相关**：pH–DO = 0.5965（中等偏强正相关）。
**最强负相关**：Temp–DO = −0.3808（弱到中等负相关）。

任务书要求的四组关系分析：
- **水温–DO（−0.3808）**：负相关。水温升高，气体溶解度下降，且高温期有机物分解耗氧增加，DO 随之下降。符合亨利定律与养殖经验。
- **pH–DO（0.5965）**：正相关，四组中最强。白天浮游植物光合作用同时吸收 CO₂（使 pH 升高）并释放 O₂（使 DO 升高），二者同向变化，故呈较强正相关。
- **水温–气温（0.3674）**：正相关。气温通过热交换影响水温，但水体热容量大、响应滞后，故相关性中等而非极强。
- **水温–盐度（−0.3316）**：负相关。高温伴随蒸发使盐度上升的理论在此被减弱——近海养殖受降雨/换水影响大，降雨同时降温和降盐，使二者呈负相关。

---

## 六、模块四：预测模型

### 6.1 单因素线性回归（对应 4.1）

`data_service_linear_regression`（[service/analyze.c](service/analyze.c:219)）基于最小二乘法拟合 `DO = a·x + b`，输出斜率 a、截距 b、决定系数 R²，并用留出法（前 80% 训练、后 20% 测试）计算 RMSE。

任务书要求的主模型（气温→DO）及多因子对比，实测：

| 自变量 | 斜率 a | 截距 b | R² | RMSE | 样本数 |
| --- | --- | --- | --- | --- | --- |
| **Air_temp（主模型）** | 0.004363 | 4.469480 | 0.000045 | 2.864593 | 23203 |
| Temp | −0.425336 | 15.514721 | 0.144993 | 3.160348 | 23203 |
| **pH（最优）** | 8.435794 | −65.507250 | **0.355845** | 1.997835 | 23203 |
| Salinity | −0.157880 | 10.027867 | 0.003996 | 2.920571 | 23203 |

**多因子对比结论**：pH 对 DO 的解释力最强（R²=0.356），远高于水温（0.145）、盐度（0.004）和气温（≈0）。这与相关性分析一致（pH–DO 相关 0.5965 为最高）。气温作为主模型 R²≈0.000045，几乎无线性解释力，预测效果差。

### 6.2 预测准确度分析（分析讨论 4.1(4)）

单因素线性回归在本数据集上预测准确度**普遍偏低**，最优的 pH→DO 模型 R² 也仅 0.356（即仅解释了 35.6% 的方差），主模型气温→DO R² 几乎为 0。原因分析：

1. **真实关系非线性**。DO 受水温（溶解度，非线性亨利定律）、光合作用（日周期）、呼吸作用、风浪曝气、投饵增氧等多因素共同影响，单因子线性模型表达能力不足。
2. **强时间自相关性被忽略**。DO 是时间序列，当前值强烈依赖前几个时刻的值（5 分钟采样间隔），而线性回归把每条记录当独立样本，未利用时间依赖信息。
3. **主模型选型不当**。气温对 DO 的直接影响弱（相关系数仅 0.0067），任务书虽指定气温→DO，但实测表明它不是好的预测因子。
4. **未考虑交互作用**。如"高温+低 pH"才真正预示缺氧风险，单因子模型无法表达交互项。

**改进方向（对应选做 4.2）**：① 多元线性回归，纳入水温、pH、气温等共同预测；② 引入时间特征（小时、日周期）和滞后项，使用 ARIMA 等时间序列模型；③ 对非线性关系使用多项式回归或决策树等非线性模型；④ 在凌晨缺氧场景下，用"前几小时 DO 下降速率"做短时预测更具实用价值。

---

## 七、模块五：系统集成

### 7.1 用户登录与权限管理（对应 5.1）

`data_service_validate_login`（[service/auth.c](service/auth.c)）预设两账户：

| 角色 | 用户名 | 密码 | 权限 |
| --- | --- | --- | --- |
| 管理员 | admin | 123456 | 全部功能 |
| 访客 | guest | guest | 仅数据概览 / 预警 / 分析报告 |

登录失败 3 次自动退出；密码以星号回显；主菜单按角色动态显示可用选项。测试验证了正确登录、错误密码、不存在用户、大小写敏感、尾空格等用例（见测试报告模块 1）。

### 7.2 菜单交互界面（对应 5.2）

[controller/DataController.c](controller/DataController.c) 实现菜单循环；[view/](view/) 各文件实现登录界面、主菜单、子菜单与表格显示。主菜单 9 项功能 + 退出，每个功能模块有独立子菜单，执行后返回主菜单，输入错误提示并重新显示。程序启动自动 `chcp 65001` 解决中文乱码。

### 7.3 系统集成检查（对应 5.3）

- **功能检查**：所有模块整合于同一工程；主菜单正确显示；各菜单项正确调用对应 service；子菜单可正常返回；退出有确认；未加载数据时访问功能有错误提示。
- **代码检查**：多文件组织（.c/.h 分离）；头文件均用 `#ifndef/#define/#endif` 条件编译防重复包含；注释充分、命名规范（`data_service_*` 服务层前缀、`Data*` 数据结构）。

---

## 八、测试

完整的单元/集成测试见 [test/测试报告.md](test/测试报告.md)，测试程序 [test/test_suite.c](test/test_suite.c)。覆盖 10 个模块、96 项功能断言（每模块含正确 + 错误用例）+ 2500 次扩容压力测试，**全部通过（2596/2596 ALL PASS）**。

测试要点：
- **正确用例**验证合法/边界输入的返回值与字段写入符合预期。
- **错误用例**验证越界值、空指针、缺失值、格式错误、空数据集、越界页码等异常输入下系统安全返回错误码或 `NAN`/`NULL`，未崩溃。
- **数值正确性**：皮尔逊相关系数、回归参数在完全线性数据上精度达 1e-6。

---

## 九、结论与改进

### 9.1 完成情况

本系统按要求完成了全部五大模块：数据基础操作（动态数组、CSV 容错读取、CSV/二进制存储与性能对比、分页/筛选/排序查询、增删改与备份恢复）、数据预处理（异常检测剔除/修复、均值逼近法缺失填充、移动平均滤波）、统计分析（基本统计量、凌晨缺氧与盐度突变预警、6×6 相关系数矩阵）、预测模型（气温→DO 单因素回归 + 多因子对比 + R²/留出法 RMSE 评估）、系统集成（登录鉴权、权限控制、菜单交互）。所有分析讨论环节均基于真实数据实测给出。

### 9.2 主要结论

- 数据质量：23,203 条中缺失 279 个、异常 475 个，无整条删除，预处理后数据完整可用。
- 风险预警：共 85 条预警（严重缺氧 30、亚缺氧 8、盐度突变 1h 45、24h 2），集中于一季度凌晨与暴雨期。
- 相关性：pH–DO 最强正相关（0.5965），Temp–DO 最强负相关（−0.3808）。
- 预测：单因素线性回归准确度偏低，pH→DO 最优（R²=0.356），主模型气温→DO 几乎无效（R²≈0），需引入多因子/时间序列模型改进。

### 9.3 改进方向

1. 引入多元线性回归 / 时间序列模型（ARIMA）/ 多项式回归提升 DO 预测精度。
2. 异常值检测补充 3σ/IQR 统计方法，识别"在范围内但异常"的离群点。
3. view 层交互与 controller 调度流程目前未纳入自动化测试，可补充端到端集成测试。
4. 选做项（用户注册、密码密文存储、技术人员角色）可作为后续扩展。
