# 海水养殖水质数据分析系统用户手册（CLI 版）

## 1. 系统简介

本系统是一个 C 语言命令行程序，用于处理海水养殖水质 CSV 数据。程序支持登录校验、数据概览、预处理、查询、统计分析、异常预警、溶解氧预测、移动平均滤波、修改记录、删除记录和添加记录。

当前版本只保留黑窗口命令行使用方式，不包含网页界面和服务器。

## 2. 数据字段

CSV 数据包含以下 6 个主要水质字段：

| 字段 | 含义 |
| --- | --- |
| `temp` | 水温 |
| `salinity` | 盐度 |
| `ph` | pH 值 |
| `do` | 溶解氧 |
| `precipitation` | 降水量 |
| `air_temp` | 气温 |

## 3. 编译程序

### 3.1 GCC 编译

```bash
gcc -std=c11 -Wall -Wextra -I. main.c service/DataService.c -lm -o water_quality.exe
```

### 3.2 CMake 编译

```bash
cmake -S . -B build
cmake --build build
```

如果本机没有安装 CMake 或 GCC，请先安装 MinGW、MSYS2、CLion 工具链或其他 C 编译环境。

## 4. 基本命令格式

```bash
./water_quality.exe <command> [options]
```

查看支持的命令：

```bash
./water_quality.exe --help
```

## 5. 登录

默认账号：

| 角色 | 用户名 | 密码 |
| --- | --- | --- |
| 管理员 | `admin` | `123456` |
| 访客 | `guest` | `guest` |

示例：

```bash
./water_quality.exe login --username admin --password 123456
```

## 6. 数据概览

```bash
./water_quality.exe overview --input dao/data_modify.csv
```

该命令读取 CSV 文件并输出记录数量、字段情况和数据概览 JSON。

## 7. 数据预处理

```bash
./water_quality.exe preprocess --input dao/data_modify.csv --output dao/data_clean.csv --marks dao/data_clean_marks.csv
```

说明：

- `--input`：输入 CSV 文件。
- `--output`：清洗后的输出 CSV 文件。
- `--marks`：预处理标记输出文件。

## 8. 查询数据

查询原始数据：

```bash
./water_quality.exe query --input dao/data_modify.csv --view raw
```

查询处理后数据：

```bash
./water_quality.exe query --input dao/data_clean.csv --view processed
```

## 9. 统计分析

```bash
./water_quality.exe stats --input dao/data_modify.csv
```

该命令输出各水质字段的均值、最小值、最大值、标准差等统计结果。

## 10. 异常预警

```bash
./water_quality.exe warnings --input dao/data_modify.csv
```

该命令用于检查溶解氧偏低、盐度突降等风险情况。

## 11. 溶解氧预测

```bash
./water_quality.exe predict --input dao/data_modify.csv
```

该命令基于现有数据进行回归预测，并输出预测结果 JSON。

## 12. 移动平均滤波

```bash
./water_quality.exe filter --input dao/data_modify.csv --output dao/data_filtered.csv --window 5
```

`--window` 支持 `3`、`5`、`7`、`9`、`11`。

## 13. 修改记录

```bash
./water_quality.exe modify --input dao/data_modify.csv --row 1 --field ph --value 8.0
```

常用 `--field` 参数：

- `temp`
- `salinity`
- `ph`
- `do`
- `precipitation`
- `air_temp`

## 14. 删除记录

```bash
./water_quality.exe delete --input dao/data_modify.csv --row 1
```

## 15. 添加记录

```bash
./water_quality.exe add --input dao/data_modify.csv --temp 25 --salinity 34 --ph 8.1 --do 6 --precipitation 0 --air_temp 27
```

## 16. 推荐操作流程

```bash
./water_quality.exe overview --input dao/data_modify.csv
./water_quality.exe preprocess --input dao/data_modify.csv --output dao/data_clean.csv --marks dao/data_clean_marks.csv
./water_quality.exe query --input dao/data_clean.csv --view processed
./water_quality.exe stats --input dao/data_clean.csv
./water_quality.exe warnings --input dao/data_clean.csv
./water_quality.exe predict --input dao/data_clean.csv
```

## 17. 常见问题

### Q1：运行时提示找不到命令？

请确认已经成功编译出 `water_quality.exe`，并且当前终端位于项目根目录或可执行文件所在目录。

### Q2：输出为什么是 JSON？

程序统一输出 JSON，既方便在终端查看，也方便以后被脚本或其他程序调用。

### Q3：中文显示乱码怎么办？

Windows 终端建议使用 UTF-8 编码，或在 PowerShell / Windows Terminal 中运行程序。
