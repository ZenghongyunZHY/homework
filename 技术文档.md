# 海水养殖水质数据分析系统 — 技术文档

## 1. 项目概述

本系统使用 **C 语言（C11 标准）** 实现海水养殖水质数据的全流程分析，覆盖数据读取、预处理、统计分析、风险预警、线性回归预测、数据维护与备份恢复。处理对象为水温、盐度、pH、溶解氧、降水量、气温共 6 项水质参数，数据规模约 2.3 万条时序记录。

程序以终端交互菜单（黑窗口）方式运行，启动后先登录鉴权，再通过数字菜单调度各功能模块。

---

## 2. 技术栈

| 类别 | 选型 | 说明 |
| --- | --- | --- |
| 编程语言 | C (ISO/IEC 9899:2011, `-std=c11`) | 纯 C 实现，无第三方运行时依赖 |
| 数学库 | `<math.h>` + `libm` | 提供 `sqrt`、`NAN`、`INFINITY`、`isnan` 等 |
| 标准库 | `<stdio.h> <stdlib.h> <string.h> <time.h> <ctype.h> <errno.h>` | 文件 I/O、内存管理、字符串处理、时间格式化 |
| 平台 API | Win32 `CreateDirectoryA` / `WIN32_FIND_DATAA` / `localtime_s`；POSIX `dirent.h` / `mkdir` / `localtime_r` | 通过 `_WIN32` 宏条件编译，跨平台支持目录与时间操作 |
| 构建工具 | CMake ≥ 4.0 + GCC/MSVC | `CMakeLists.txt` 统一管理源文件与链接 |
| 编码约定 | UTF-8 源码，启动 `chcp 65001` | 解决 Windows 终端中文乱码 |

**核心数据结构全部自实现**：动态数组、链表队列、动态字符串数组，未引入任何外部容器库。

---

## 3. 系统架构

系统采用经典的**三层架构（Layered Architecture）**，职责严格分离：

```
┌─────────────────────────────────────────────┐
│  View 层 (view/*.c)      终端交互、表格渲染   │
│    view_login / view_crud / view_analyze     │
│    view_report / view_backup / view_util     │
├─────────────────────────────────────────────┤
│  Controller 层 (controller/) 状态管理与调度  │
│    AppState 全局状态、菜单循环、流程编排      │
├─────────────────────────────────────────────┤
│  Service 层 (service/*.c)  核心业务与算法    │
│    io_csv / io_marks / field_meta / dataset  │
│    analyze / auth / storage_backup / reports │
├─────────────────────────────────────────────┤
│  POJO 层 (pojo/*.h)       纯数据结构定义     │
│    Data / DataSet / User / Node / Queue      │
├─────────────────────────────────────────────┤
│  DAO 层 (dao/)            数据文件存储       │
│    data_modify.csv / data_clean.csv / 备份    │
└─────────────────────────────────────────────┘
```

- **入口** `main.c` 仅调用 `controller_run_interactive()`，不包含业务逻辑。
- **Controller** 维护 `AppState` 结构体，集中管理当前用户、原始/清洗数据集、查询参数、预处理标记等全部会话状态，并串联「加载 → 预处理 → 分析 → 报告」流程。
- **Service** 是无状态的算法库，所有函数以 `DataSet*` 等参数显式传入数据，便于单元测试与复用。
- **依赖方向** 单向自上而下：View → Controller → Service → POJO，下层不感知上层。

---

## 4. 数据设计

### 4.1 核心记录 `Data`（[pojo/Data.h:10](pojo/Data.h:10)）

```c
typedef struct {
    int record_index;       // 原始行号（1-based），时序索引
    double temp;            // 水温 (°C)
    double salinity;        // 盐度 (PSU)
    double ph;              // pH
    double do_value;        // 溶解氧 (mg/L)
    double precipitation;   // 降水量 (mm)
    double air_temp;        // 气温 (°C)
} Data;
```

- 采用**定长字段结构体**，6 个 `double` 连续内存布局，可直接整体 `fwrite`/`fread` 实现二进制存储，无需序列化开销。
- 缺失值统一以 IEEE 754 `NAN` 表示，与 `isnan()` 配合贯穿全部分析流程，避免用哨兵值（如 -999）混淆真实数据。

### 4.2 动态数组 `DataSet`（[pojo/Data.h:20](pojo/Data.h:20)）

```c
typedef struct {
    Data* items;
    int   size;
    int   capacity;
} DataSet;
```

- **扩容策略**（[service/dataset.c:33](service/dataset.c:33)）：初始容量 1000，满载后容量 ×2 倍增，通过 `realloc` 整体迁移。倍增策略保证均摊 O(1) 的 `push` 复杂度。
- 提供 `init / free / push` 三件套，所有动态集合（`MarkSet`、`WarningSet`、`OperationMarkSet`）复用同一套 `ds_dyn_reserve` 扩容内核。

### 4.3 字段元数据 `FieldMeta`（[service/field_meta.c:13](service/field_meta.c:13)）

```c
static const FieldMeta FIELDS[WQ_FIELD_COUNT] = {
    {"temp",         "Temp",         "degC", -5.0,  40.0},
    {"salinity",     "Salinity",     "PSU",   0.0,  45.0},
    {"ph",           "pH",           "",      6.5,  9.0},
    {"do",           "DO",           "mg/l",  0.0,  15.0},
    {"precipitation","Precipitation","mm",    0.0, 500.0},
    {"air_temp",     "Air temp",     "degC",-10.0,  50.0}
};
```

将**字段名、显示标签、单位、合法取值范围**集中为元数据表，配合 `data_service_get_field_value` / `set_field_value`（switch 分发），实现字段访问的**数据驱动**：异常检测、统计、排序、筛选均通过字段索引 (0–5) 通用化，新增字段只需修改元数据表与 switch。

### 4.4 预处理标记 `RowMark` / `OperationMark`

- `RowMark{row, action, missing_count, abnormal_count}`：记录每行预处理动作（`none/fill/repair/repair_fill/delete`），落盘 `dao/data_marks.csv`。
- `OperationMark{row, status, fields[6]}`：记录用户修改/删除操作，落盘 `dao/data_op_marks.json`，支持按操作类型回溯查询。

### 4.5 用户与权限 `User`（[pojo/User.h:7](pojo/User.h:7)）

```c
typedef struct { char* name; char* password; int role; } User;  // role: 1=管理员 0=访客
```

内置 `admin`/`guest` 两账号，登录失败 3 次自动退出，访客仅可查看概览/预警/报告。

### 4.6 持久化文件布局

| 文件 | 格式 | 内容 |
| --- | --- | --- |
| `dao/data_modify.csv` | CSV | 原始水质数据 |
| `dao/data_clean.csv` | CSV | 预处理后清洗数据 |
| `dao/data_filtered.csv` | CSV | 移动平均滤波结果 |
| `dao/data_marks.csv` | CSV | 预处理行级标记 |
| `dao/data_op_marks.json` | JSON | 用户操作标记 |
| `backup/backup_YYYYMMDD_HHMMSS.csv` | CSV | 带时间戳的手动备份 |
| `reports/*.txt` | 文本 | 概览/统计/预警/预测报告 |

---

## 5. 算法思想

### 5.1 CSV 解析与缺失值识别（[service/io_csv.c](service/io_csv.c)）

逐行 `fgets` 读取，按逗号切分 token，`strtod` 解析浮点。对空串、`nan`、`-999`、`-9999` 统一识别为缺失值并置 `NAN`；解析失败计入 `format_errors` 但不中断，保证容错性。读入时即赋予 `record_index`，作为后续时序定位的主键。

### 5.2 数据预处理（[service/analyze.c:33](service/analyze.c:33)）

预处理分两阶段：

**阶段一 — 异常值检测与剔除**
- 逐字段判断值是否落在 `FieldMeta` 合法范围内。
- **超 3 个字段异常**的记录判定为脏数据，整体删除（`deleted_records`）。
- 异常但未超阈的字段置为 `NAN`，转为缺失值处理。

**阶段二 — 均值逼近法缺失值填充**
- 先计算各字段全局均值 `global_means`。
- 对每个缺失位置，取其前后各 10 步（共最多 20 个邻居）的有效值求局部均值填充；若邻域全为缺失，则回退到全局均值。

该**邻域加权均值法**优于直接用全局均值：时序数据具有局部相关性，邻域均值能更好保留局部趋势，避免全局填充造成的均值平滑偏差。

### 5.3 移动平均滤波（[service/analyze.c:283](service/analyze.c:283)）

对前 4 个时序字段（水温/盐度/pH/溶解氧）做窗口大小可调（3/5/7/9/11）的**中心对称滑动平均**：

$$\hat{x}_i = \frac{1}{2h+1}\sum_{k=i-h}^{i+h} x_k$$

- 边界处窗口自动截断，仅对有效值求均值。
- 滤波前后分别计算标准差，通过对比量化去噪效果。移动平均是低通滤波器，能有效抑制高频噪声而保留趋势。

### 5.4 基本统计量（[service/analyze.c:83](service/analyze.c:83)）

单遍历计算 `count / mean / min / max`，第二遍计算标准差（总体标准差，除以 N）：

$$\sigma = \sqrt{\frac{1}{N}\sum_{i}(x_i-\bar{x})^2}$$

全程跳过 `NAN`，使用 `INFINITY`/`-INFINITY` 初始化最值，避免首元素特殊处理。

### 5.5 皮尔逊相关系数（[service/analyze.c:115](service/analyze.c:115)）

单遍累加 `sum_x / sum_y / sum_x2 / sum_y2 / sum_xy`，套用公式：

$$r = \frac{n\sum xy - \sum x \sum y}{\sqrt{(n\sum x^2-(\sum x)^2)(n\sum y^2-(\sum y)^2)}}$$

构建 6×6 对称相关矩阵，仅算上三角即可。该实现为**数值稳定的一次遍历**版，O(n) 时间完成，分母为 0 时返回 `NAN` 防止除零。

### 5.6 线性回归预测（[service/analyze.c:219](service/analyze.c:219)）

以气温（或多因子）为自变量 X、溶解氧为因变量 Y，最小二乘法求解：

$$\hat{y} = \beta_1 x + \beta_0,\quad \beta_1 = \frac{n\sum xy - \sum x \sum y}{n\sum x^2 - (\sum x)^2},\quad \beta_0 = \bar{y} - \beta_1\bar{x}$$

**模型评估双指标**：
- **决定系数 R²** = $1 - SS_{res}/SS_{tot}$，衡量拟合优度。
- **留出法 RMSE**：前 80% 数据训练，后 20% 数据测试，计算 $\sqrt{\frac{1}{m}\sum(y_i-\hat{y}_i)^2}$，衡量泛化误差。

留出法避免了用训练集自身评估的过拟合偏差，R² 与 RMSE 互补：前者看解释力，后者看预测误差量级。系统还对水温/pH/盐度/气温 4 个因子分别建模，对比哪个因子对溶解氧预测力最强。

### 5.7 风险预警（[service/analyze.c:158](service/analyze.c:158)）

数据按 5 分钟一条时序采集，以 `2025-01-01 12:00` 为基准时间，`record_index` 换算为绝对时刻。

**凌晨缺氧预警**：逐日聚合 03:00–05:00（minute_of_day ∈ [180,300]）的溶解氧均值：
- 均值 < 3.0 → `severe_low_oxygen`（严重缺氧，建议投放粒氧）
- 均值 < 4.0 → `low_oxygen`（亚缺氧，建议开启底增氧）

**盐度突变预警**：
- 1 小时（12 条记录）下降 > 2.0 PSU → `salinity_drop_1h`
- 24 小时（288 条记录）下降 > 5.0 PSU → `salinity_drop_24h`

阈值基于养殖学经验：盐度骤变破坏渗透压平衡，溶解氧夜间低谷易致窒息。

### 5.8 查询：筛选 + 排序 + 分页（[service/analyze.c:339](service/analyze.c:339)）

1. **筛选**：按字段值区间过滤 + 按预处理/操作标记类型过滤，结果存入临时 `DataSet`。
2. **排序**：用标准库 `qsort`，比较函数通过模块级变量 `g_sort_field/g_sort_desc` 传递排序键与方向（C 的 `qsort` 不支持上下文的变通方案），`NAN` 一律排末尾。
3. **分页**：每页默认 15 条，支持上/下页与跳转；`total_pages = ceil(total/page_size)`，按起止下标 `memcpy` 切片返回。

整体复杂度 O(n) 筛选 + O(n log n) 排序，对 2.3 万条数据毫秒级响应。

### 5.9 存储性能对比（[service/storage_backup.c:52](service/storage_backup.c:52)）

同份数据分别以 CSV（文本）与二进制（`fwrite` 整块 `Data` 数组）写入，用 `clock()` 测量读写耗时，对比文件体积与 I/O 速度，直观展示二进制存储在紧凑度与速度上的优势。

### 5.10 备份与恢复（[service/storage_backup.c:111](service/storage_backup.c:111)）

- 备份：以 `backup_YYYYMMDD_HHMMSS.csv` 命名，8KB 缓冲区流式复制源文件，保留修改/删除前的历史快照。
- 列举：Win32 `FindFirstFileA` / POSIX `opendir` 列出 `backup/` 下匹配 `backup_*.csv` 的文件。
- 恢复：从指定备份 CSV 重新读入 `DataSet`。

---

## 6. 关键设计要点小结

1. **数据驱动字段访问**：元数据表 + switch 分发，6 个字段共用一套通用算法，扩展性强。
2. **NAN 统一缺失语义**：从读入到分析全程使用 IEEE 754 `NAN`，避免哨兵值污染统计。
3. **均摊 O(1) 动态数组**：倍增扩容策略支撑 2.3 万级数据零拷贝增长。
4. **两阶段预处理**：先剔除再填充，邻域均值保留时序局部性。
5. **留出法模型评估**：R² + RMSE 双指标，避免过拟合误导。
6. **三层解耦**：Service 无状态、可测；Controller 集中状态；View 纯交互。
7. **跨平台条件编译**：`_WIN32` 宏隔离平台 API，同一份代码兼容 Windows/POSIX。
8. **操作可回溯**：`RowMark` + `OperationMark` 双标记体系，支持按预处理动作与用户操作类型追溯数据变更历史。
